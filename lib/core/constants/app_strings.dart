/// Centralized user-facing strings. Replace with l10n lookups if
/// multi-language support is added (see lib/l10n/).
class AppStrings {
  AppStrings._();

  static const String appName = 'Harraka';
}
